package com.matrimonyapp

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder JVM unit test. Its only purpose in Phase 01 is to prove that the
 * Kotlin unit-test source set compiles and runs.
 */
class AppSmokeTest {

    @Test
    fun arithmeticSanity() {
        assertEquals(4, 2 + 2)
    }
}
