# JVM used by the Gradle daemon. JDK 17 is the AGP 9.1 minimum and default.
org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8

org.gradle.parallel=true
org.gradle.caching=true

# AndroidX is mandatory for this project.
android.useAndroidX=true

# Deliberately NOT set (per Phase 01 constraints):
#   android.builtInKotlin  -> left at AGP 9 default (true)
#   android.newDsl         -> left at AGP 9 default
